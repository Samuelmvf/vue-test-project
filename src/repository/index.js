/**
 * Interface IRepository
 * - cadastrar(dados)
 * - editar(id, dados)
 * - buscarPorId(id)
 * - buscarTodos()
 * - deletar(id)
 */

export { default as ClienteRepository } from './cliente-repository'
export { default as ProdutoRepository } from './produto-repository'
